# Browser demo mode

Set `NEXT_PUBLIC_DEMO_MODE=true` to run the original CRM interface with hardcoded browser-resolved data.

The demo preserves the production UI components, charts, tables, typography, spacing, navigation, and theme. It bypasses authentication and server-side prefetching, and it never calls the CRM API or database.
